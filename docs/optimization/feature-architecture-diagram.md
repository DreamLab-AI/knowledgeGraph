# WasmVOWL Enhanced Features - Architecture Diagram

---

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        WasmVOWL Enhanced Architecture                │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                            UI Layer (React)                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐    │
│  │  TopMenuBar     │  │    Sidebar      │  │  GraphCanvas    │    │
│  │  - Export       │  │  - Search (F1)  │  │  - 3D Rendering │    │
│  │  - View Toggle  │  │  - Filters (F2) │  │  - Interactions │    │
│  │  - Statistics   │  │  - Details (F5) │  │  - LOD (F10)    │    │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘    │
│           │                    │                     │              │
│           └────────────────────┴─────────────────────┘              │
│                                │                                     │
└────────────────────────────────┼─────────────────────────────────────┘
                                 │
                   ┌─────────────▼─────────────┐
                   │   State Management Layer  │
                   │      (Zustand + Immer)    │
                   ├───────────────────────────┤
                   │                           │
                   │  ┌──────────────────┐    │
                   │  │ useGraphStore    │    │
                   │  │ - Nodes/Edges    │    │
                   │  │ - Filters (F2)   │    │
                   │  │ - Statistics(F7) │    │
                   │  │ - Sessions (F8)  │    │
                   │  └─────────┬────────┘    │
                   │            │              │
                   │  ┌─────────▼────────┐    │
                   │  │ useUIStore       │    │
                   │  │ - Search (F1)    │    │
                   │  │ - Clustering(F3) │    │
                   │  │ - Layouts (F6)   │    │
                   │  │ - Performance(F10)│   │
                   │  └─────────┬────────┘    │
                   │            │              │
                   └────────────┼──────────────┘
                                │
                   ┌────────────▼──────────────┐
                   │   WASM Bridge Layer       │
                   │   (useWasmSimulation)     │
                   ├───────────────────────────┤
                   │                           │
                   │  • Data conversion        │
                   │  • Position updates       │
                   │  • Layout parameters      │
                   │  • Clustering config (F3) │
                   │  • Statistics calls (F7)  │
                   │                           │
                   └────────────┬──────────────┘
                                │
                   ┌────────────▼──────────────┐
                   │   WASM Core (Rust)        │
                   │   (webvowl_wasm.wasm)     │
                   ├───────────────────────────┤
                   │                           │
                   │  ┌────────────────────┐  │
                   │  │ Graph Engine       │  │
                   │  │ - Node/Edge Storage│  │
                   │  │ - Spatial Index    │  │
                   │  └────────────────────┘  │
                   │                           │
                   │  ┌────────────────────┐  │
                   │  │ Layout Algorithms  │  │
                   │  │ - Force (default)  │  │
                   │  │ - Hierarchical(F6) │  │
                   │  │ - Circular (F6)    │  │
                   │  │ - Tree (F4)        │  │
                   │  └────────────────────┘  │
                   │                           │
                   │  ┌────────────────────┐  │
                   │  │ Clustering (F3)    │  │
                   │  │ - Domain Detection │  │
                   │  │ - Cluster Forces   │  │
                   │  └────────────────────┘  │
                   │                           │
                   │  ┌────────────────────┐  │
                   │  │ Analytics (F7)     │  │
                   │  │ - Centrality       │  │
                   │  │ - Communities      │  │
                   │  │ - Path Lengths     │  │
                   │  └────────────────────┘  │
                   │                           │
                   └───────────────────────────┘
```

---

## Feature Dependency Graph

```
Feature Dependencies (arrows show "requires" relationship)

                    ┌──────────────┐
                    │ Base System  │
                    │ (Current)    │
                    └──────┬───────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
    ┌──────▼──────┐ ┌─────▼──────┐ ┌─────▼──────┐
    │   Feature 2 │ │ Feature 5  │ │ Feature 10 │
    │ Edge Filter │ │ Node Panel │ │Performance │
    │  (Easy)     │ │  (Easy)    │ │  (Easy)    │
    └──────┬──────┘ └────────────┘ └────────────┘
           │
           │
    ┌──────▼──────┐
    │  Feature 1  │
    │   Search    │
    │  (Medium)   │
    └──────┬──────┘
           │
           ├────────────┐
           │            │
    ┌──────▼──────┐ ┌──▼────────┐
    │  Feature 3  │ │ Feature 7 │
    │ Clustering  │ │Statistics │
    │  (Medium)   │ │  (Easy)   │
    └─────────────┘ └───────────┘
           │
           │
    ┌──────▼──────┐
    │  Feature 4  │
    │ Hierarchy   │
    │   (Hard)    │
    └──────┬──────┘
           │
           ├────────────┐
           │            │
    ┌──────▼──────┐ ┌──▼────────┐
    │  Feature 6  │ │ Feature 8 │
    │  Layouts    │ │ Sessions  │
    │  (Medium)   │ │ (Medium)  │
    └─────────────┘ └───────────┘
           │
           │
    ┌──────▼──────┐
    │  Feature 9  │
    │   Export    │
    │  (Medium)   │
    └─────────────┘
```

---

## Component Interaction Diagram

```
User Interaction Flow

┌─────────────────────────────────────────────────────────────┐
│                         User Actions                         │
└─────────────────────────────────────────────────────────────┘
     │             │              │              │
     │ Search      │ Filter       │ Click Node   │ Adjust Settings
     │             │              │              │
     ▼             ▼              ▼              ▼
┌─────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐
│ Search  │  │  Edge    │  │  Node    │  │Performance │
│  Bar    │  │ Filter   │  │ Details  │  │   Panel    │
│ (F1)    │  │  (F2)    │  │  (F5)    │  │   (F10)    │
└────┬────┘  └────┬─────┘  └────┬─────┘  └─────┬──────┘
     │            │             │               │
     │            │             │               │
     ▼            ▼             ▼               ▼
┌─────────────────────────────────────────────────────┐
│             State Updates (Zustand)                 │
│                                                     │
│  • searchResults     • visibleEdgeTypes            │
│  • selectedNode      • lodSettings                 │
│  • filters           • layoutParams                │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│         React Re-render (Selective)                 │
│                                                     │
│  • Highlighted nodes (F1)                          │
│  • Hidden edges (F2)                               │
│  • Details panel (F5)                              │
│  • LOD changes (F10)                               │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│         WASM Simulation Update                      │
│                                                     │
│  • Clustering forces (F3)                          │
│  • Layout algorithm (F6)                           │
│  • Position calculations                           │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│         WebGL Rendering (R3F)                       │
│                                                     │
│  • Node positions                                  │
│  • Edge visibility                                 │
│  • Visual effects (LOD, highlighting)              │
└─────────────────────────────────────────────────────┘
```

---

## Data Flow - Search Feature (F1)

```
┌──────────┐
│ User     │
│ Types    │
│ "Neural" │
└────┬─────┘
     │
     ▼
┌─────────────────┐
│ SearchBar.tsx   │ ← React Component
│ - onChange      │
│ - debounce 300ms│
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ useUIStore      │ ← State Update
│ .setSearchQuery │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Search Worker   │ ← Web Worker (async)
│ - Build index   │
│ - Rank results  │
│ - Return top 100│
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ useUIStore      │ ← State Update
│ .searchResults  │
│ = [ids...]      │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ ClassNode.tsx   │ ← Re-render affected nodes
│ - isSearchMatch │
│ - highlight     │
│ - scale up      │
└─────────────────┘
```

---

## Data Flow - Domain Clustering (F3)

```
┌──────────┐
│ User     │
│ Toggles  │
│Clustering│
└────┬─────┘
     │
     ▼
┌─────────────────────┐
│DomainClusterPanel   │ ← React Component
│ - Toggle switch     │
│ - Strength slider   │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ useUIStore          │ ← State Update
│ .clusteringEnabled  │
│ .clusterStrength    │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ useWasmSimulation   │ ← Hook detects change
│ - Read new settings │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ WASM Bridge         │ ← JavaScript → Rust
│ wasm.setClustering  │
│ (enabled, strength) │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ WASM Engine (Rust)  │ ← Rust Execution
│ 1. Detect domains   │
│ 2. Calc cluster ⊕  │
│ 3. Apply forces     │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ Position Updates    │ ← Rust → JavaScript
│ - New node.x, y, z  │
└────┬────────────────┘
     │
     ▼
┌─────────────────────┐
│ React Re-render     │ ← Visual Update
│ - Nodes move        │
│ - Clusters separate │
└─────────────────────┘
```

---

## Storage Architecture - Session Management (F8)

```
┌──────────────────────────────────────────────────────┐
│                 Session Storage                       │
└──────────────────────────────────────────────────────┘

                    User Actions
                         │
          ┌──────────────┼──────────────┐
          │              │              │
    ┌─────▼─────┐  ┌────▼────┐  ┌─────▼─────┐
    │   Save    │  │  Load   │  │  Export   │
    └─────┬─────┘  └────┬────┘  └─────┬─────┘
          │             │              │
          ▼             ▼              ▼
┌─────────────────────────────────────────────────┐
│      SessionStorageManager (TypeScript)         │
│                                                 │
│  • Capture state from Zustand stores           │
│  • Serialize to JSON                           │
│  • Compress with LZ-string (80% reduction)     │
│  • Calculate checksum (integrity)              │
└────────────┬────────────────────┬───────────────┘
             │                    │
             │                    │
    ┌────────▼────────┐  ┌────────▼────────┐
    │   IndexedDB     │  │  localStorage   │
    │                 │  │                 │
    │  • Primary      │  │  • Backup       │
    │  • No size limit│  │  • 5-10 MB max  │
    │  • Async API    │  │  • Sync API     │
    └─────────────────┘  └─────────────────┘

Session Data Structure:

{
  version: "1.0",
  metadata: {
    name: "AI Exploration",
    created: "2025-11-13T14:23:00Z",
    modified: "2025-11-13T15:45:00Z"
  },
  ontology: {
    filename: "ai-ontology.ttl",
    nodeCount: 142,
    edgeCount: 287,
    checksum: "sha256-abc123..."
  },
  viewport: { zoom, rotation, target, mode },
  filters: { activeFilters, searchQuery, ... },
  layout: { algorithm, params, nodePositions? },
  selection: { selectedNode, bookmarkedNodes },
  settings: { ... },
  annotations: { notes, highlights, customColors }
}
```

---

## Export Pipeline - Enhanced Formats (F9)

```
┌──────────────────────────────────────────────────────┐
│                 Export Pipeline                       │
└──────────────────────────────────────────────────────┘

User selects:
• Export scope (full/filtered/selected)
• Export format (GraphML/GEXF/Cytoscape/CSV)
• Options (positions/metadata/annotations)

                    ▼
        ┌───────────────────────┐
        │  ExportDialog.tsx     │
        │  - Format selector    │
        │  - Options checkboxes │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ GraphExporter.ts      │
        │ - Extract subgraph    │
        │ - Convert to format   │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ Format Converters     │
        ├───────────────────────┤
        │                       │
        │ ┌─────────────────┐  │
        │ │ exportGraphML() │  │ → XML string
        │ └─────────────────┘  │
        │                       │
        │ ┌─────────────────┐  │
        │ │ exportGEXF()    │  │ → XML string
        │ └─────────────────┘  │
        │                       │
        │ ┌─────────────────┐  │
        │ │exportCytoscape()│  │ → JSON string
        │ └─────────────────┘  │
        │                       │
        │ ┌─────────────────┐  │
        │ │ exportCSV()     │  │ → CSV string
        │ └─────────────────┘  │
        │                       │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ Download              │
        │ - Create Blob         │
        │ - Trigger download    │
        │ - filename.ext        │
        └───────────────────────┘
                    ▼
        ┌───────────────────────┐
        │ External Tools        │
        ├───────────────────────┤
        │ • Gephi (GraphML)     │
        │ • Cytoscape (JSON)    │
        │ • NetworkX (GraphML)  │
        │ • D3.js (JSON)        │
        │ • Excel (CSV)         │
        └───────────────────────┘
```

---

## Performance Optimization - LOD System (F10)

```
┌──────────────────────────────────────────────────────┐
│         Level of Detail (LOD) System                  │
└──────────────────────────────────────────────────────┘

                 Camera Movement
                       │
                       ▼
        ┌──────────────────────────┐
        │ ClassNode.tsx            │
        │ - Calculate distance to  │
        │   camera                 │
        └──────────┬───────────────┘
                   │
                   ▼
        ┌──────────────────────────┐
        │ Determine Detail Level   │
        ├──────────────────────────┤
        │                          │
        │ distance < 500:          │
        │   → HIGH DETAIL          │
        │   • 32-segment circle    │
        │   • Full label (12px)    │
        │   • All properties       │
        │                          │
        │ distance 500-800:        │
        │   → MEDIUM DETAIL        │
        │   • 16-segment circle    │
        │   • Short label (10px)   │
        │   • No properties        │
        │                          │
        │ distance > 800:          │
        │   → MINIMAL DETAIL       │
        │   • 8-segment circle     │
        │   • No label             │
        │   • No properties        │
        │                          │
        └──────────┬───────────────┘
                   │
                   ▼
        ┌──────────────────────────┐
        │ Render                   │
        ├──────────────────────────┤
        │ HIGH:    150 draw calls  │
        │ MEDIUM:  75 draw calls   │
        │ MINIMAL: 30 draw calls   │
        └──────────────────────────┘

Performance Gain:
500 nodes @ HIGH:    15 FPS
500 nodes @ MEDIUM:  35 FPS
500 nodes @ MINIMAL: 60 FPS

800 nodes @ LOD mix: 60 FPS ✓
```

---

## Feature Integration Matrix

```
         F1  F2  F3  F4  F5  F6  F7  F8  F9 F10
        ┌──┬──┬──┬──┬──┬──┬──┬──┬──┬───┐
    F1  │  │✓ │✓ │  │✓ │  │  │✓ │  │   │ Search
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F2  │✓ │  │✓ │  │✓ │  │  │✓ │✓ │   │ Edge Filter
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F3  │✓ │✓ │  │✓ │  │✓ │✓ │✓ │✓ │   │ Clustering
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F4  │  │  │✓ │  │✓ │✓ │  │✓ │✓ │   │ Hierarchy
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F5  │✓ │✓ │  │✓ │  │  │  │✓ │  │   │ Node Panel
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F6  │  │  │✓ │✓ │  │  │  │✓ │  │   │ Layouts
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F7  │  │  │✓ │  │  │  │  │✓ │✓ │   │ Statistics
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F8  │✓ │✓ │✓ │✓ │✓ │✓ │  │  │  │   │ Sessions
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
    F9  │  │✓ │✓ │✓ │  │  │✓ │  │  │   │ Export
        ├──┼──┼──┼──┼──┼──┼──┼──┼──┼───┤
   F10  │  │  │  │  │  │  │  │  │  │   │ Performance
        └──┴──┴──┴──┴──┴──┴──┴──┴──┴───┘

✓ = Features that integrate/enhance each other
```

**Integration Examples:**
- **F1 + F2:** Search filtered by edge type
- **F3 + F6:** Clustering with layout algorithms
- **F5 + F4:** Node details in hierarchy view
- **F8:** Sessions save state from F1-F7
- **F9 + F3:** Export clustered subgraphs

---

## Technology Stack Integration

```
┌─────────────────────────────────────────────────────┐
│                Technology Stack                      │
└─────────────────────────────────────────────────────┘

Frontend Layer
├── React 18.3 (UI framework)
├── React Three Fiber 9.4 (WebGL)
├── TypeScript 5.9 (Type safety)
├── Zustand 5.0 (State management)
│   └── Immer 10.0 (Immutable updates)
├── Vite 7.2 (Build tool)
└── CSS Modules (Styling)

New Dependencies (Features)
├── lz-string 1.5.0 (Session compression - F8)
├── fast-xml-parser 4.3.0 (Export formats - F9)
├── web-worker (Search performance - F1)
└── indexeddb-wrapper (Session storage - F8)

Backend Layer (WASM)
├── Rust 1.75+ (Performance-critical code)
├── wasm-bindgen (JS↔Rust bridge)
├── serde (Serialization)
├── petgraph (Graph algorithms - F7)
│   ├── Betweenness centrality
│   ├── Community detection
│   └── Shortest paths
├── raphtory (Optional: Time-series graphs)
└── rayon (Parallel processing)

Browser APIs
├── WebGL (Rendering)
├── Web Workers (Async computation)
├── IndexedDB (Persistent storage)
├── localStorage (Backup storage)
└── Canvas API (Export - F9)
```

---

**Document Version:** 1.0
**Created:** 2025-11-13
**Last Updated:** 2025-11-13
