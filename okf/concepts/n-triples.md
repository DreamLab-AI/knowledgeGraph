---
okf_version: "0.2"
type: Class
title: N-Triples
resource: urn:ngm:class:n-triples
domain: data
description: N-Triples is a line-based, plain-text serialization format for RDF in which each line encodes a single subject-predicate-object triple terminated by a period. It is deliberately minimal and unambiguous, using full IRIs rather than prefixes, which makes it easy to parse, stream, and compare line by line. The format is a W3C standard and serves as a canonical interchange and testing representation f
maturity: established
quality: 0.72
is-a:
  - urn:ngm:class:technical-standard
relatedTo:
  - urn:ngm:class:turtle
  - urn:ngm:class:solid
---

# N-Triples

N-Triples is a line-based, plain-text serialization format for RDF in which each line encodes a single subject-predicate-object triple terminated by a period. It is deliberately minimal and unambiguous, using full IRIs rather than prefixes, which makes it easy to parse, stream, and compare line by line. The format is a W3C standard and serves as a canonical interchange and testing representation for RDF graphs.
